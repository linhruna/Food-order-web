# Backend configuration
PORT=4000

# Local MongoDB is the safest default to avoid DNS issues when no Atlas cluster is configured.
DB=mongodb://127.0.0.1:27017/food-web
# If using MongoDB Atlas, replace <cluster-host> with the host shown in the "Connect" dialog (e.g. cluster0.abcd123.mongodb.net)
# DB=mongodb+srv://<username>:<password>@<cluster-host>/food-web?retryWrites=true&w=majority&appName=Cluster0

JWT_SECRET=change_me_in_production
STRIPE_SECRET_KEY=sk_test_your_key
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:4173
